# Task 9 Report: Themes and cel loader

## What I implemented

Exactly per brief (`task-9-brief.md`):

- `assets/cels/raijin.svg` — copied verbatim from `docs/superpowers/specs/cels/raijin.svg`.
- `tools/embed-cels.mjs` — reads every `.svg` in `assets/cels`, collapses whitespace, and writes a generated `src/render/cels/<id>.ts` module exporting `<ID>_CEL` as a string constant.
- `package.json` — added `"cels": "node tools/embed-cels.mjs"` script.
- `src/render/cels/raijin.ts` — generated by running `npm run cels`; committed.
- `src/render/theme.ts` — `Theme` interface exactly as specified (palette, fonts, copy, jewelEvery).
- `src/render/cel.ts` — `svgDataUrl` (pure, prefixes `data:image/svg+xml;charset=utf-8,` + `encodeURIComponent`) and `loadCel` (browser-only: `Image` + `document.createElement('canvas')`, memoized by `${url}|${w}|${h}|${dpr}` key).
- `src/render/themes/raijin.ts` — `RAIJIN_THEME: Theme` built from `RAIJIN_CEL` via `svgDataUrl`.
- `src/render/themes/index.ts` — `THEMES: Record<MachineId, Theme>`; `big-wave` and `hana-fan` alias `RAIJIN_THEME` with their own `id`/`name` (Task 14 replaces these).
- `tests/theme.test.ts` — verifies every `MACHINE_ORDER` id has a theme with matching `id`, a `data:image/svg+xml` `celUrl`, ≥3 lamps, ≥1 Google font family, non-empty `copy.jackpot`, `jewelEvery > 1`, and no banned monetary terms (`coin`, `credit`, `cash`, `$`) anywhere in `copy`.

## What I tested and results

- `npx vitest run tests/theme.test.ts` — 1 test, passed (after RED confirmation below).
- `npx vitest run` (full suite) — **10 files, 75 tests, all passed** (74 prior + 1 new), including the soak (~9.1s) and balance tests. No regressions.
- `npx tsc --noEmit` — clean, no output, no errors.

## TDD evidence

**RED** — `npx vitest run tests/theme.test.ts` (before implementing `theme.ts`/`cel.ts`/`themes/`):

```
Error: Cannot find module '../src/render/themes/index' imported from '/Users/swebber64/dhg/pachinko/tests/theme.test.ts'
 FAIL  tests/theme.test.ts [ tests/theme.test.ts ]
 Test Files  1 failed (1)
      Tests  no tests
```

**GREEN** — `npx vitest run tests/theme.test.ts` (after implementation):

```
 ✓ tests/theme.test.ts (1 test) 1ms

 Test Files  1 passed (1)
      Tests  1 passed (1)
```

**Full suite GREEN**:

```
 Test Files  10 passed (10)
      Tests  75 passed (75)
   Duration  9.36s
```

## Files changed (all new except package.json)

- `assets/cels/raijin.svg` (new, copied)
- `tools/embed-cels.mjs` (new)
- `package.json` (modified — added `cels` script)
- `src/render/cels/raijin.ts` (new, generated)
- `src/render/theme.ts` (new)
- `src/render/cel.ts` (new)
- `src/render/themes/raijin.ts` (new)
- `src/render/themes/index.ts` (new)
- `tests/theme.test.ts` (new)

## Self-review findings

- Read the full diff (`git show` on the commit) line by line: every file matches the brief's code blocks verbatim — no typos, no added/removed fields, no extra abstractions.
- `git add` list matches the brief's Step 7 exactly (same files, same order).
- Confirmed `RAIJIN_THEME.copy` contains no banned substrings (`coin`, `credit`, `cash`, `$`) — verified by the passing test's explicit assertion.
- Confirmed `big-wave`/`hana-fan` alias comment is present and matches the brief's wording, flagging Task 14 as the follow-up.
- No unrelated files touched; no dead code left behind.
- `loadCel` is correctly left untested per the brief's note (browser-only `Image`/`document` API) — no unit test attempts to exercise it, only `svgDataUrl` is exercised indirectly via the theme test.

## Concerns

None. Task is complete, scoped exactly to the brief, and green end-to-end.
