# Wave E report — UI, audio, tests

Worktree: `/Users/swebber64/dhg/pachinko/.claude/worktrees/agent-a7b86ad550a2bbbe3` (branch `worktree-agent-a7b86ad550a2bbbe3`, from `a4adf14` on `feature/pachinko-v1`).
Commit: `2dc306e` — `fix(ui): app test harness, real sound cap, audio cleanup, alloc-free effects, embed whitespace, render metrics, dial/swipe/texture/e2e fixes`.

Files touched (all within the allowlist): `src/app.ts`, `src/audio/synth.ts`, `src/input/dial.ts`, `src/panel/panel.css`, `src/render/{canvas,cel,effects,theme}.ts`, `tools/embed-cels.mjs`, `e2e/smoke.spec.ts`, `package.json` + `package-lock.json` (jsdom devDependency only), `tests/effects.test.ts`, new `tests/app.test.ts`, `tests/app-harness.ts`, `tests/embed.test.ts`. No changes to `src/core/**`, `tools/balance.ts`, or the core tests. Cels regenerated byte-identical. Screenshots not re-captured (no visible change at 1440×900, see E11/E13).

## Per item

**E1 — App unit-test harness.** `jsdom@^30.0.1` added as a devDependency. `tests/app-harness.ts` stubs `ResizeObserver` (no-op class), `matchMedia` (`{matches:false, add/removeEventListener}`), `requestAnimationFrame`/`cancelAnimationFrame` (manual queue with `raf.pump(now)`), `AudioContext` (`FakeAudioContext`, no-op nodes), `HTMLCanvasElement.prototype.getContext` (a Proxy whose every property is a method returning the proxy, so gradients chain), `Image` (resolves `onload` synchronously on `src` set; `failingImages` set drives `onerror`), and `Element.prototype.setPointerCapture`. `mount()` builds an App on a fresh root over `MemoryStorage`, starts it and returns `step(seconds)` which pumps the rAF queue with explicit timestamps (50 ms frames). No `_frameForTest` was needed on App. `tests/app.test.ts` has the `// @vitest-environment jsdom` header and 14 tests (list below).

**E2 — Sound cap counts every note.** `Synth.arp` now calls `allow()` per note; the jackpot-loop `tick` calls `allow()` per tick and always increments `loop.i`, so a denied tick is skipped but the melody position and the `setInterval` beat do not drift. The callers for `catch`, `reachStart`, `jackpotOpen`, `attackerCatch`, `jackpotClose` no longer pre-consume one `allow()` slot for the whole arpeggio (that was double-counting once notes are counted individually). Tests in `tests/effects.test.ts` ("Synth cap"): 50 catch chimes → exactly 12 oscillators created; jackpot loop under an exhausted cap → 12 oscillators through the first second while `loop.i` advances 1→2, then 15 oscillators and `loop.i` 5 after the window rolls over (raijin loop is 500 ms/tick).

**E3 — Audio node cleanup.** `beep()` sets `o.onended = () => { o.disconnect(); g.disconnect(); }`. Test: "disconnects oscillator and gain when a note ends".

**E4 — Effects per-frame allocations.** The three `filter()` calls in `Effects.update` are replaced by write-index compaction (`this.particles[n++] = p; this.particles.length = n`) for particles, floats and flashes. Test: "Effects.update compaction > keeps live particles and drops expired ones in place" (11 particles / 1 float / 1 flash survive a 10 ms step, flash expires at 100 ms, all gone after 1 s).

**E5 — loadCel rejection.** `p.catch(() => cache.delete(key))` attached right after `cache.set`. First attempt put the delete inside `onerror`, which the harness exposed as wrong: when `onerror` fires synchronously (inside the Promise executor) the delete ran before `cache.set`, and the rejected promise was cached anyway. Test: "loadCel > does not cache a rejected promise" (fails once, then resolves to a canvas).

**E6 — embed-cels whitespace.** `tools/embed-cels.mjs` now exports `collapseSvg(svg)` and `embedAll()`, and only runs `embedAll` when executed directly. The collapse strips comments, tokenizes on tags, collapses whitespace inside tags and in text between tags, and leaves text nodes untouched while inside a `<text>` element or any element with `xml:space="preserve"` (stack-tracked, nested children included). `tests/embed.test.ts` runs the transform on a fixture with `<text>a  b</text>`, a newline inside `<text>`, and a `<g xml:space="preserve">` with a nested `<tspan>`, asserting each survives and that attribute runs/inter-tag whitespace collapse. `node tools/embed-cels.mjs` regenerated `src/render/cels/*.ts` with no diff (existing labels already had single spaces); `tests/theme.test.ts` passes.

**E7 — Render metrics as data.** `RENDER_METRICS` exported from `src/render/theme.ts` with a comment: `tulip {pedestalW 16, pedestalH 14, wingRoot 7, wingBulge 4, wingLift 10, wingLen 20, wingWidth 4, labelPx 9, labelDy 7}`, `pocket {h 16, lip 2, labelPx 10, labelDy 6}`, `attacker {closedH 8, openH 14, lip 4}`, `reelFontRatio 0.8`. `canvas.ts` imports it as `RM` and uses it in the attacker, reel font and `drawCatcher` paths. Same pixels as before.

**E8 — Dial touch-action.** `Dial` stores `zone.style.touchAction` before setting `none` and restores it in `destroy()`. Test: "Dial restores the previous touch-action on destroy" (board pre-set to `pan-y` comes back as `pan-y`; dial goes back to empty).

**E9 — App.game getter.** `private _game` + `get game(): Game`. Test: "App.game is a read-only getter" (descriptor has `get`, no `set`; assignment throws).

**E10 — Swipe clamps.** `onSwipe` indexes `MACHINE_ORDER[idx + dir]` and ignores an undefined result. Test: "swipe clamps at the ends of MACHINE_ORDER instead of wrapping" (raijin ← stays, → big-wave → hana-fan → stays, ← big-wave), driven by pointer events on `.pk-tabs`.

**E11 — Gutter texture fallback.** `.pk-board` base rule uses `repeating-linear-gradient(45deg, rgba(255,255,255,.06) 0 3px, transparent 3px 14px)`; the `color-mix()` version is inside `@supports (color: color-mix(in srgb, red, blue))`. Chromium computed style at 1440×900 still resolves to the color-mix stripe (`color(srgb 0.83 0.63 0.09 / 0.06)`), so the reference look is unchanged.

**E12 — Mute e2e.** `page.waitForTimeout(700)` replaced by `page.waitForFunction(() => (localStorage.getItem('pachinko.save.v1') ?? '').includes('"mute":true'))`.

**E13 — Desktop panel headroom.** `overflow-y: auto` added to `.pk-panel` in the `@container (min-width: 1024px)` block. Scratch verification (Playwright, raijin seed 7): 1280×600 → panel 578 px tall, scrollHeight 578, Reset bottom 577 (fits exactly, no scroll needed); 1280×560 → panel 538, scrollHeight 563, Reset bottom moves 577 → 552 after scrolling (reachable, not clipped); 1440×900 → panel 878, scrollHeight 878, layout identical to `docs/superpowers/screenshots/task13-desktop.png`. Zero console errors in all three. Scratch PNGs: `/private/tmp/claude-501/-Users-swebber64-dhg-pachinko/7e27fb76-2f43-4ad5-af66-a7f5d4fc31c9/scratchpad/desktop-{1280x600,1280x560,1440x900}.png`.

## Test names

`tests/app.test.ts` (14):
- App lifecycle › start creates the shell, panel and canvas without console errors
- App lifecycle › stop() before start() resolves does not throw and registers nothing
- App lifecycle › stop() after start removes every window/document listener it added and disposes the synth
- App lifecycle › App.game is a read-only getter
- App bank, overlays and saves › Reset restores bank 100 and persists it
- App bank, overlays and saves › buy-in overlay appears when the bank is empty with no balls in flight, and buyIn refills
- App bank, overlays and saves › switching machines persists the previous save and loads the next
- App bank, overlays and saves › requestSwitch is a no-op for the current machine and during jackpot
- App bank, overlays and saves › the first-run overlay is dismissed by the first launch and firstRunDone is saved
- App bank, overlays and saves › mute toggle persists (button and M key)
- App attract mode › starts after 20 s idle, fires free balls (bank unchanged), stops on activity
- App input › swipe clamps at the ends of MACHINE_ORDER instead of wrapping
- App input › Dial restores the previous touch-action on destroy
- loadCel › does not cache a rejected promise

`tests/effects.test.ts` (+4): Effects.update compaction › keeps live particles and drops expired ones in place; Synth cap › schedules at most 12 beeps per second across 50 catch chimes (every arpeggio note counts); Synth cap › jackpot loop keeps its beat while the cap denies ticks; Synth cap › disconnects oscillator and gain when a note ends.

`tests/embed.test.ts` (3): keeps the double space inside `<text>`; keeps whitespace under xml:space="preserve", including nested children; collapses runs between tags and inside attribute runs, drops comments.

## Commands / output

- `npm ci` — ok.
- `npm install --save-dev jsdom@30` — `"jsdom": "^30.0.1"`.
- `node tools/embed-cels.mjs` — 3 cels embedded; `git status -- src/render/cels` clean (byte-identical).
- `npx tsc --noEmit` — clean.
- `npx vitest run` — `Test Files 13 passed (13)`, `Tests 119 passed (119)` (was 98; +21).
- `npm run e2e` — `12 passed (27.9s)`, all "no console errors" assertions green.
- Scratch screenshots — see E13; `errors: []` at every size.

## Concerns

- E2 changes audible behaviour slightly by design: a chime that lands when the second's budget is spent now drops individual notes rather than the whole arpeggio; loop ticks under load are silent but stay on the beat.
- The `Synth` node-env tests stub `window` as `globalThis` because `startLoop` uses `window.setInterval`; that is a test-side accommodation, not a source change.
- `tests/embed.test.ts` imports the `.mjs` script under a `// @ts-expect-error` (no declaration file; `allowJs` is off). If `allowJs` is ever enabled the directive becomes an unused-directive error and should be removed.
- E13: the review's "<620 tall clips" turned out to be "<600" with the current panel (578 px + 22 px marquee); scrolling is verified at 560.
- Report written inside the worktree because the sandbox refused the main-checkout `.superpowers/` path.
